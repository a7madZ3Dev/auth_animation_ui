import 'package:flutter/material.dart';

const Color loginBg = Color.fromRGBO(32, 73, 105, 1);
// Color(0xFF00C470)
const Color signUpBg = Color.fromRGBO(138, 198, 209, 1);
// Color(0xFF000A54);

const double defaultPadding = 16.0;
const Duration defaultDuration = Duration(milliseconds: 300);
